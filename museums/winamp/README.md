# Winamp Timeline

WordPress releases as tracks in a music-player timeline. The complete interface
lives in `index.html` and uses `../data/releases.js` and the shared local fonts.

Run `sfw npm start` from the repository root and open `/winamp/`.
There are no experience-specific dependencies or build steps. `museum.json`
lists the public files. See the root README for checks and deployment.
